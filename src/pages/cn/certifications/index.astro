---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getSiteContent } from '../../../lib/siteContent';

const lang = 'cn' as const;
const content = getSiteContent(lang);
const canonicalPath = '/cn/certifications/';
const alternates = { cn: '/cn/certifications/', en: '/en/certifications/' };
---

<BaseLayout
  lang={lang}
  title={`${content.siteName}｜${content.nav.certifications}`}
  description="企业资质证书、检测报告与项目案例展示。"
  canonicalPath={canonicalPath}
  alternates={alternates}
>
  <section class="section">
    <div class="container">
      <div class="h1">{content.nav.certifications}</div>
      <div class="grid grid-2" style="margin-top:16px;">
        <div class="panel">
          <div class="h2">资质证书</div>
          <div class="muted">放置证书图片或 PDF，建议按类型分组并注明有效期与编号。</div>
        </div>
        <div class="panel">
          <div class="h2">检测报告</div>
          <div class="muted">放置第三方检测或内部检验报告摘要，并提供下载链接。</div>
        </div>
      </div>
      <div class="panel" style="margin-top:16px;">
        <div class="h2">项目案例</div>
        <div class="muted">以“行业 + 场景 + 交付内容”方式描述，不包含敏感数据。</div>
      </div>
    </div>
  </section>
</BaseLayout>
