---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getSiteContent } from '../../../lib/siteContent';

const lang = 'cn' as const;
const content = getSiteContent(lang);
const canonicalPath = '/cn/products/';
const alternates = { cn: '/cn/products/', en: '/en/products/' };
---

<BaseLayout
  lang={lang}
  title={`${content.siteName}｜${content.nav.products}`}
  description="按系列展示，支持参数 PDF 下载与定制沟通。"
  canonicalPath={canonicalPath}
  alternates={alternates}
>
  <section class="section">
    <div class="container">
      <div class="h1">{content.nav.products}</div>
      <div class="muted" style="max-width:860px;">
        按“系列”展示，不展示实时价格。参数以 PDF 形式提供下载，支持定制与工况适配沟通。
      </div>
      <div class="grid grid-3" style="margin-top:16px;">
        <a class="panel panel-link" href="/cn/products/wire-rope-sling/">
          <div class="h2">钢丝绳索具</div>
          <div class="muted">{content.cta.getSpec} →</div>
        </a>
        <a class="panel panel-link" href="/cn/products/piling-machine-parts/">
          <div class="h2">桩机配件</div>
          <div class="muted">{content.cta.getSpec} →</div>
        </a>
        <a class="panel panel-link" href="/cn/products/custom-capability/">
          <div class="h2">定制与能力</div>
          <div class="muted">{content.cta.contact} →</div>
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
